<div class="news">
  {% if site.news != blank %}
    {% assign news_size = site.news | size %}
    <div
      class="table-responsive"
      {% if include.limit and site.announcements.scrollable and news_size > 3 %}
        style="max-height: 60vw"
      {% endif %}
    >
      <table class="table table-sm table-borderless">
        {% if include.limit and site.announcements.limit %}
          {% assign news_limit = site.announcements.limit %}
        {% else %}
          {% assign news_limit = news_size %}
        {% endif %}

        <tr> <th scope="row" style="width: 20%">{{ '2024-01-20'| date: '%b %d, %Y' }}</th>
          <td> {{ "Received the Best Researcher Award from the Department of Applied AI at SKKU! 🙌" | remove: '<p>' | remove: '</p>' | emojify }} </td>
        </tr>

        <tr> <th scope="row" style="width: 20%">{{ '2023-12-10'| date: '%b %d, %Y' }}</th>
          <td> {{ "Delighted to have presented an oral at EMNLP 2023 in Singapore :sparkles: :smile:" | remove: '<p>' | remove: '</p>' | emojify }}</td>
        </tr>

        <tr> <th scope="row" style="width: 20%">{{ '2023-07-31'| date: '%b %d, %Y' }}</th>
          <td> {{ "Honored to be supported by the Korean government as one of the 10 elite young female STEM researchers. 🎉" | remove: '<p>' | remove: '</p>' | emojify }}</td>
        </tr>

        <tr> <th scope="row" style="width: 20%">{{ '2023-07-15'| date: '%b %d, %Y' }}</th>
          <td> {{ "Awarded  KDD Student Travel Award! 🎉" | remove: '<p>' | remove: '</p>' | emojify }}</td>
        </tr>


      </table>
    </div>
  {% else %}
    <p>No news so far...</p>
  {% endif %}
</div>